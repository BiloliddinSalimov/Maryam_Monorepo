{
  "name": "maryam-monorepo",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "install-all": "pnpm install"
  },
  "devDependencies": {
    "turbo": "^2.8.21"
  }
}
